const {chromium}=require('playwright'),assert=require('node:assert/strict'),path=require('node:path'),fs=require('node:fs');
(async()=>{const browser=await chromium.launch({headless:true,channel:'chrome'});try{const page=await browser.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));const url=process.env.THREE_THINGS_SITE||'file://'+path.resolve(__dirname,'../docs/index.html');for(const width of [390,900,1400]){await page.setViewportSize({width,height:1000});await page.goto(url);await page.locator('img').evaluate(img=>img.decode());assert.equal(await page.locator('h1').count(),1);assert.equal(await page.locator('.download').count(),2);assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await page.locator('a[href="#install"]').click();assert(await page.locator('#install').isVisible());const out=path.resolve(__dirname,'../evidence-v010');fs.mkdirSync(out,{recursive:true});await page.screenshot({path:path.join(out,'page-'+width+'.png'),fullPage:true});}assert.deepEqual(errors,[]);console.log('PASS download page: 390/900/1400px, image decoding, download links, install anchor, no overflow or page errors; '+url);}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
